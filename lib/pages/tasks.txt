favoritos: 
- mudar os containers para pegar os itens dos restaurantes favoritos
- tornar os container dos restaurantes clicaveis, redirecionando para o perfil do restaurante

Pedidos: 
- mudar os containers para pegar os ultimos pedidos feitos
- colocar o estado do pedido

Home:
- mudar os containers para pegar os itens dos restaurantes 
- tonar os containers clicaveis, redirecionando para cada restaurante

perfil:
- criar a tela de perfil do usuario
- fazer cada campo pegar a informação do usuario correspondente

pagar: 
- criar a tela de pagamento
- fazer funcionar

/*
body: SafeArea(
        // desabilita o efeito que aparece quando rola tudo pra cima ou tudo pra baixo
        child: NotificationListener<OverscrollIndicatorNotification>(
          onNotification: (notification) {
            notification.disallowGlow();
            return true;
          },
          child: SingleChildScrollView(
            child: 
          ),
        ),
      ),
*/